describe('Create documents', () => {

  xit('should create a root folder', function () {
    cy.visit('/form');
  });

  xit('should create a folder below a root folder', function () {

  });

  xit('should create a folder by switching location in dialog to \'Daten\' when a root folder was selected initially', function () {

  });

  xit('should create a folder by switching location in dialog to a sub folder when no node was selected initially', function () {

  });

  xit('should create a folder from the dashboard', function () {

  });
})
